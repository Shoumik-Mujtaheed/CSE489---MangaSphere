name: mangasphere
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  archive: ^4.0.7
  cached_network_image: ^3.4.1  # cache online images 
  carousel_slider: ^5.1.1       # for paginating infinite scrolling 
  cloud_firestore: ^6.0.1       # firestore package 
  file_picker: ^10.3.2          # for importing files from android device 
  firebase_auth: ^6.0.2         # for user authentication 
  firebase_core: ^4.1.0         
  firebase_database: ^12.0.1    # firestore database 
  
  flutter:
    sdk: flutter
  http: ^1.5.0
  json_annotation: ^4.9.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  json_serializable: ^6.11.0
  build_runner: ^2.7.1
  

flutter:
  uses-material-design: true
